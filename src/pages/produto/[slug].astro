---
// [slug].astro - Detalhes do Produto Fiel à Identidade Visual Artools PRO
import Layout from '../../layouts/Layout.astro';
import Button from '../../components/Button.astro';
import { products, type PenProduct } from '../../data/products';

export async function getStaticPaths() {
  return products.map((p) => ({
    params: { slug: p.slug || p.id.toLowerCase() },
    props: { product: p }
  }));
}

const { slug } = Astro.params;
const { product } = Astro.props as { product?: PenProduct };

const defaultProduct = product || products.find(p => (p.slug || p.id).toLowerCase() === slug?.toLowerCase() || p.id.toLowerCase() === slug?.toLowerCase()) || products[0];
---

<Layout
    title={`${defaultProduct.name} | Avise-me Quando Disponível — ArtTools`}
    description={`Conheça a caneta ${defaultProduct.name} (${defaultProduct.code}) em ${defaultProduct.material}. Inscreva-se para receber o alerta de disponibilidade e lote exclusivo.`}
    canonicalURL={new URL(Astro.url.pathname + (Astro.url.search || ''), Astro.site || Astro.url.origin).href}
    image={defaultProduct.image}
>
    <main class="page-white-bg">
        <div class="product-page-container">
            <!-- Breadcrumbs -->
            <nav class="product-breadcrumbs" aria-label="Navegação estrutural">
                <a href="/" class="crumb-link">INÍCIO</a>
                <span class="crumb-sep">/</span>
                <a href="/catalogo" class="crumb-link">CATÁLOGO</a>
                <span class="crumb-sep">/</span>
                <span class="crumb-active" id="crumb-active-name">{defaultProduct.name.toUpperCase()}</span>
            </nav>

            <!-- Grid Principal de 2 Colunas -->
            <section class="product-layout-grid" aria-label="Apresentação do Produto">
                <!-- ==============================================================
                     COLUNA ESQUERDA: GALERIA DE FOTOS + ITENS INCLUSOS ABAIXO
                     ============================================================== -->
                <div class="product-gallery-column">
                    <div class="product-gallery-wrapper">
                        <!-- Moldura da Imagem Principal com Badges -->
                        <div class="product-hero-frame">
                            <div class="frame-badges-overlay">
                                <span class="badge-pill badge-dark" id="hero-badge-left">NOVO LANÇAMENTO</span>
                                <span class="badge-pill badge-light" id="hero-badge-right">{defaultProduct.status.toUpperCase()}</span>
                            </div>

                            <div class="hero-image-wrapper">
                                <img
                                    id="gallery-main-img"
                                    src={defaultProduct.image}
                                    alt={`Instrumento ${defaultProduct.name}`}
                                    loading="eager"
                                    fetchpriority="high"
                                />
                            </div>
                        </div>

                        <!-- Miniaturas das Fotos (4 Quadrados) -->
                        <div class="gallery-thumbs-row" aria-label="Miniaturas de fotos">
                            <button type="button" class="thumb-square active" data-img="/assets/images/pen_stealth.jpg" aria-label="Foto 1: Titanium Stealth">
                                <img src="/assets/images/pen_stealth.jpg" alt="Acabamento Titânio" loading="lazy" />
                            </button>
                            <button type="button" class="thumb-square" data-img="/assets/images/pen_ceramic.jpg" aria-label="Foto 2: Ceramic White">
                                <img src="/assets/images/pen_ceramic.jpg" alt="Acabamento Cerâmica Branca" loading="lazy" />
                            </button>
                            <button type="button" class="thumb-square" data-img="/assets/images/pen_blue.jpg" aria-label="Foto 3: Cobalt Aero">
                                <img src="/assets/images/pen_blue.jpg" alt="Versão Azul Cobalto" loading="lazy" />
                            </button>
                            <button type="button" class="thumb-square" data-img="/assets/images/pen_silver.jpg" aria-label="Foto 4: Flux Silver">
                                <img src="/assets/images/pen_silver.jpg" alt="Versão Prata Escovada" loading="lazy" />
                            </button>
                        </div>
                    </div>

                    <!-- ITENS INCLUSOS ABAIXO DAS IMAGENS (conforme solicitado pelo usuário) -->
                    <div class="inbox-section-below">
                        <h2 class="inbox-heading">ITENS INCLUSOS NO ESTOJO</h2>
                        <ul class="inbox-bullet-list" id="inbox-items-list">
                            {defaultProduct.inBox.map((item) => (
                                <li>
                                    <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                                        <polyline points="20 6 9 17 4 12"></polyline>
                                    </svg>
                                    <span>{item}</span>
                                </li>
                            ))}
                        </ul>
                    </div>
                </div>

                <!-- ==============================================================
                     COLUNA DIREITA: INFORMAÇÕES DE COMPRA + AVISE-ME
                     ============================================================== -->
                <div class="product-purchase-column">
                    <div class="purchase-header-block">
                        <!-- Pre-título em Pill Badge com bolinha verde -->
                        <div class="purchase-eyebrow">
                            <span class="badge-dot" aria-hidden="true"></span>
                            <span id="purchase-eyebrow-text">PRECISÃO & ENGENHARIA</span>
                        </div>

                        <!-- Título da Caneta -->
                        <h1 class="purchase-title" id="purchase-product-title">{defaultProduct.name}</h1>

                        <!-- Descrição Breve -->
                        <p class="purchase-brief-desc" id="purchase-brief-desc">{defaultProduct.utility}</p>
                    </div>

                    <!-- Preço Exclusivo e Lote Futuro -->
                    <div class="purchase-pricing-row">
                        <div class="price-stack">
                            <span class="price-tiny-label">PREÇO EXCLUSIVO</span>
                            <span class="price-main-val" id="purchase-price">{defaultProduct.price}</span>
                        </div>
                        <span class="batch-future-tag">LOTE FUTURO</span>
                    </div>

                    <!-- Especificações Rápidas e Minimalistas (3 Colunas no Box Cinza Claro) -->
                    <div class="quick-specs-container">
                        <div class="quick-specs-label">ESPECIFICAÇÕES RÁPIDAS</div>
                        <div class="quick-specs-grid">
                            <div class="quick-spec-item">
                                <span class="spec-term">PENA</span>
                                <span class="spec-def" id="spec-tip">{defaultProduct.tip}</span>
                            </div>
                            <div class="quick-spec-item">
                                <span class="spec-term">MATERIAL</span>
                                <span class="spec-def" id="spec-mat">{defaultProduct.material}</span>
                            </div>
                            <div class="quick-spec-item">
                                <span class="spec-term">RECARGA</span>
                                <span class="spec-def" id="spec-refill">{defaultProduct.refill || 'Rollerball G2'}</span>
                            </div>
                        </div>
                    </div>

                    <!-- Seletor de Modelo (Cards com Nome e Status) -->
                    <div class="model-selection-wrapper">
                        <div class="selection-label">SELECIONE O MODELO</div>

                        <!-- 1ª Linha: Apenas os 3 Modelos Iniciais -->
                        <div class="model-cards-row" id="model-primary-row">
                            {products.slice(0, 3).map((p) => (
                                <a
                                    href={`/produto/${p.slug || p.id.toLowerCase()}`}
                                    class={`model-select-card ${p.id === defaultProduct.id ? 'active' : ''}`}
                                    data-model-id={p.id}
                                    aria-label={`Selecionar modelo ${p.name}`}
                                >
                                    <span class="model-select-name">{p.name}</span>
                                    <span class="model-select-status">{p.status}</span>
                                </a>
                            ))}
                        </div>

                        <!-- Botão Ver mais / Ver menos posicionado diretamente abaixo da 1ª linha -->
                        <div class="model-selection-footer">
                            <button
                                type="button"
                                class="btn-toggle-more-models"
                                id="toggle-more-models-btn"
                                aria-expanded="false"
                                aria-label="Alternar exibição de mais modelos"
                            >
                                <span class="btn-toggle-text">Ver mais modelos (6)</span>
                                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="btn-toggle-chevron">
                                    <polyline points="6 9 12 15 18 9"></polyline>
                                </svg>
                            </button>
                        </div>

                        <!-- 2ª Linha (Escondida por padrão): Os 6 Modelos Restantes -->
                        <div class="model-cards-row-more hidden" id="model-secondary-row">
                            {products.slice(3).map((p) => (
                                <a
                                    href={`/produto/${p.slug || p.id.toLowerCase()}`}
                                    class={`model-select-card ${p.id === defaultProduct.id ? 'active' : ''}`}
                                    data-model-id={p.id}
                                    aria-label={`Selecionar modelo ${p.name}`}
                                >
                                    <span class="model-select-name">{p.name}</span>
                                    <span class="model-select-status">{p.status}</span>
                                </a>
                            ))}
                        </div>
                    </div>

                    <!-- Botão CTA Principal: AVISE-ME QUANDO DISPONÍVEL -->
                    <div class="purchase-action-block">
                        <Button
                            variant="primary"
                            id="open-aviseme-btn"
                            ariaLabel="Abrir formulário de aviso de estoque"
                        >
                            <span>AVISE-ME QUANDO DISPONÍVEL</span>
                            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path>
                                <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
                            </svg>
                        </Button>
                    </div>

                     </div>
            </section>

            <!-- ==============================================================
                 ESPECIFICAÇÕES TÉCNICAS DETALHADAS (ABAJO)
                 ============================================================== -->
            <section class="product-specs-detailed-section" aria-label="Especificações Técnicas Completas">
                <div class="specs-section-header">
                    <h2 class="specs-section-title">ESPECIFICAÇÕES TÉCNICAS DE FABRICAÇÃO</h2>
                    <span class="specs-section-subtitle">Normas metrológicas Suíça & Aeroespacial</span>
                </div>

                <div class="specs-table-grid">
                    <div class="specs-table-col">
                        <div class="spec-row">
                            <span class="spec-key">Chassi & Liga</span>
                            <span class="spec-val" id="spec-full-mat">{defaultProduct.material}</span>
                        </div>
                        <div class="spec-row">
                            <span class="spec-key">Mecânica de Trava</span>
                            <span class="spec-val" id="spec-full-mech">{defaultProduct.mechanism}</span>
                        </div>
                        <div class="spec-row">
                            <span class="spec-key">Dimensões Físicas</span>
                            <span class="spec-val" id="spec-full-dim">{defaultProduct.dimensions}</span>
                        </div>
                    </div>

                    <div class="specs-table-col">
                        <div class="spec-row">
                            <span class="spec-key">Tratamento de Superfície</span>
                            <span class="spec-val" id="spec-full-finish">{defaultProduct.finish}</span>
                        </div>
                        <div class="spec-row">
                            <span class="spec-key">Tolerância de Usinagem</span>
                            <span class="spec-val" id="spec-full-tol">{defaultProduct.tolerance}</span>
                        </div>
                        <div class="spec-row">
                            <span class="spec-key">Peso & Balanço</span>
                            <span class="spec-val" id="spec-full-weight">{defaultProduct.weight} ({defaultProduct.balance})</span>
                        </div>
                    </div>
                </div>
            </section>
        </div>
    </main>

    <!-- ==============================================================
         MODAL / POPUP: AVISE-ME QUANDO DISPONÍVEL
         ============================================================== -->
    <div class="aviseme-modal-backdrop hidden" id="aviseme-modal" role="dialog" aria-modal="true" aria-labelledby="popup-heading">
        <div class="aviseme-modal-card">
            <button type="button" class="modal-close-btn" id="close-modal-btn" aria-label="Fechar janela">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="18" y1="6" x2="6" y2="18"></line>
                    <line x1="6" y1="6" x2="18" y2="18"></line>
                </svg>
            </button>

            <div class="modal-content-state" id="modal-form-state">
                <div class="modal-eyebrow">
                    <span class="badge-dot"></span>
                    <span>LISTA VIP // LOTE EXCLUSIVO 2026</span>
                </div>

                <h2 class="popup-title" id="popup-heading">Avise-me quando disponível</h2>
                <p class="modal-desc">
                    Cadastre-se para receber notificação em primeira mão assim que o novo lote de <strong id="modal-product-name">{defaultProduct.name}</strong> for liberado.
                </p>

                <form class="aviseme-form" id="aviseme-form">
                    <div class="form-field">
                        <label for="user-name-input" class="field-label">NOME COMPLETO</label>
                        <input type="text" id="user-name-input" class="field-input" placeholder="Seu nome..." required />
                    </div>

                    <div class="form-field">
                        <label for="user-email-input" class="field-label">E-MAIL VIP</label>
                        <input type="email" id="user-email-input" class="field-input" placeholder="seu.email@exemplo.com" required />
                    </div>

                    <button type="submit" class="modal-submit-btn">
                        <span>CONFIRMAR INSCRIÇÃO VIP</span>
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                            <line x1="5" y1="12" x2="19" y2="12"></line>
                            <polyline points="12 5 19 12 12 19"></polyline>
                        </svg>
                    </button>
                </form>
            </div>

            <!-- Estado de Sucesso (Oculto por padrão) -->
            <div class="modal-content-state hidden" id="modal-success-state">
                <div class="success-icon-badge">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                </div>
                <h3 class="popup-title">Inscrição Confirmada!</h3>
                <p class="modal-desc">
                    Você foi inserido na lista prioritária da Arttools Pro. Você receberá um e-mail de confirmação com os detalhes de acesso antecipado.
                </p>
                <button type="button" class="modal-submit-btn" id="finish-modal-btn">
                    <span>CONCLUÍDO</span>
                </button>
            </div>
        </div>
    </div>
</Layout>

<script is:inline>
    (function initProductPage() {
        function setup() {
            const toggleBtn = document.getElementById('toggle-more-models-btn');
            const secondaryRow = document.getElementById('model-secondary-row');
            
            if (toggleBtn && secondaryRow) {
                toggleBtn.onclick = function(e) {
                    if (e) e.preventDefault();
                    const isHidden = secondaryRow.classList.contains('hidden') || secondaryRow.style.display === 'none';
                    if (isHidden) {
                        secondaryRow.classList.remove('hidden');
                        secondaryRow.style.display = 'grid';
                        toggleBtn.setAttribute('aria-expanded', 'true');
                        const textSpan = toggleBtn.querySelector('.btn-toggle-text');
                        if (textSpan) textSpan.textContent = 'Ver menos modelos';
                    } else {
                        secondaryRow.classList.add('hidden');
                        secondaryRow.style.display = 'none';
                        toggleBtn.setAttribute('aria-expanded', 'false');
                        const textSpan = toggleBtn.querySelector('.btn-toggle-text');
                        if (textSpan) textSpan.textContent = 'Ver mais modelos (6)';
                    }
                };
            }

            const modal = document.getElementById('aviseme-modal');
            const openBtn = document.getElementById('open-aviseme-btn');
            const closeBtn = document.getElementById('close-modal-btn');
            const finishBtn = document.getElementById('finish-modal-btn');
            const avisemeForm = document.getElementById('aviseme-form');
            const formState = document.getElementById('modal-form-state');
            const successState = document.getElementById('modal-success-state');

            function openModal(e) {
                if (e) e.preventDefault();
                const targetModal = document.getElementById('aviseme-modal');
                if (targetModal) {
                    targetModal.classList.remove('hidden');
                    targetModal.style.display = 'flex';
                    document.body.style.overflow = 'hidden';
                }
            }

            function closeModal(e) {
                if (e) e.preventDefault();
                const targetModal = document.getElementById('aviseme-modal');
                if (targetModal) {
                    targetModal.classList.add('hidden');
                    targetModal.style.display = 'none';
                    document.body.style.overflow = '';
                    if (formState && successState) {
                        formState.classList.remove('hidden');
                        formState.style.display = 'flex';
                        successState.classList.add('hidden');
                        successState.style.display = 'none';
                    }
                }
            }

            if (openBtn) openBtn.onclick = openModal;
            if (closeBtn) closeBtn.onclick = closeModal;
            if (finishBtn) finishBtn.onclick = closeModal;

            if (avisemeForm) {
                avisemeForm.onsubmit = function(e) {
                    e.preventDefault();
                    if (formState && successState) {
                        formState.classList.add('hidden');
                        formState.style.display = 'none';
                        successState.classList.remove('hidden');
                        successState.style.display = 'block';
                    }
                };
            }

            // Document click delegation for opening modal and thumbnails
            document.addEventListener('click', function(e) {
                const trigger = e.target.closest('#open-aviseme-btn') || e.target.closest('.btn-primary');
                if (trigger && trigger.innerText && trigger.innerText.includes('AVISE-ME')) {
                    openModal(e);
                }

                const thumb = e.target.closest('.thumb-square');
                if (thumb) {
                    document.querySelectorAll('.thumb-square').forEach(t => t.classList.remove('active'));
                    thumb.classList.add('active');
                    const imgUrl = thumb.getAttribute('data-img');
                    const mainImg = document.getElementById('gallery-main-img');
                    if (mainImg && imgUrl) {
                        mainImg.src = imgUrl;
                    }
                }
            });
        }

        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', setup);
        } else {
            setup();
        }
        document.addEventListener('astro:page-load', setup);
    })();
</script>

<style is:global>
        /* Fundo 100% Branco Puro e Remoção de Texturas */
        html,
        body {
            background-color: #FFFFFF !important;
        }

        .noise-layer,
        .grid-guide {
            display: none !important;
        }

        /* 1. Remoção total de sublinhados azuis */
        a,
        a:visited,
        a:hover,
        a:focus,
        .crumb-link,
        .model-select-card,
        .model-select-name,
        .model-select-status {
            text-decoration: none !important;
        }

        .page-white-bg {
            background-color: #FFFFFF !important;
            min-height: 100vh;
            padding-top: 5.5rem;
            padding-bottom: 5rem;
        }

        .product-page-container {
            max-width: 1240px;
            margin: 0 auto;
            padding: 1.5rem 2rem;
        }

        /* Breadcrumbs Minimalistas */
        .product-breadcrumbs {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-family: var(--font-mono, monospace);
            font-size: 0.72rem;
            font-weight: 600;
            letter-spacing: 0.08em;
            color: #8C8C88;
            margin-bottom: 2.2rem;
        }

        .crumb-link {
            color: #8C8C88;
            text-decoration: none !important;
            transition: color 0.2s ease;
        }

        .crumb-link:hover {
            color: #1C1C1C;
        }

        .crumb-sep {
            color: #D1D1CD;
        }

        .crumb-active {
            color: #1C1C1C;
            font-weight: 700;
        }

        /* Grid Principal de 2 Colunas */
        .product-layout-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr);
            gap: 3.5rem;
            align-items: start;
            width: 100%;
        }

        @media (max-width: 960px) {
            .product-layout-grid {
                grid-template-columns: 1fr;
                gap: 2.5rem;
            }
        }

        /* Coluna Esquerda: Galeria e Itens Inclusos */
        .product-gallery-column {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            min-width: 0;
            width: 100%;
        }

        .product-hero-frame {
            position: relative;
            background: #000000;
            border-radius: 4px;
            overflow: hidden;
            width: 100%;
        }

        .frame-badges-overlay {
            position: absolute;
            top: 1rem;
            left: 1rem;
            right: 1rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            z-index: 2;
        }

        .badge-pill {
            font-family: var(--font-mono, monospace);
            font-size: 0.65rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            padding: 0.3rem 0.65rem;
            border-radius: 3px;
        }

        .badge-dark {
            background: #1C1C1C;
            color: #FFFFFF;
        }

        .badge-light {
            background: rgba(255, 255, 255, 0.92);
            color: #1C1C1C;
            border: 1px solid rgba(0, 0, 0, 0.1);
        }

        .hero-image-wrapper {
            width: 100%;
            height: 480px;
            background: #0A0A0A;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        @media (max-width: 640px) {
            .hero-image-wrapper {
                height: 340px;
            }
        }

        .hero-image-wrapper img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
            display: block;
            transition: transform 0.4s ease;
        }

        .product-hero-frame:hover .hero-image-wrapper img {
            transform: scale(1.02);
        }

        /* 4 Miniaturas Quadradas */
        .gallery-thumbs-row {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 0.75rem;
        }

        .thumb-square {
            aspect-ratio: 1 / 1;
            background: #F4F4F2;
            border: 1px solid #E5E5E0;
            border-radius: 4px;
            padding: 0;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .thumb-square img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }

        .thumb-square:hover,
        .thumb-square.active {
            border-color: #1C1C1C;
            box-shadow: 0 0 0 1.5px #1C1C1C;
        }

        /* Itens Inclusos */
        .inbox-section-below {
            background: #F9F9F8;
            border: 1px solid #EAEAE7;
            border-radius: 6px;
            padding: 1.25rem 1.4rem;
            margin-top: 0.5rem;
        }

        .inbox-heading {
            font-family: var(--font-mono, monospace);
            font-size: 0.68rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            color: #70706E;
            text-transform: uppercase;
            margin: 0 0 0.85rem 0;
            padding-bottom: 0.4rem;
            border-bottom: 1px solid #EAEAE7;
        }

        .inbox-bullet-list {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .inbox-bullet-list li {
            display: flex;
            align-items: center;
            gap: 0.6rem;
            font-size: 0.82rem;
            color: #3A3A38;
            line-height: 1.4;
        }

        .inbox-bullet-list li svg {
            color: #1C1C1C;
            flex-shrink: 0;
        }

        /* Coluna Direita: Informações de Compra */
        .product-purchase-column {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            min-width: 0;
            width: 100%;
        }

        .purchase-eyebrow {
            display: inline-flex;
            align-items: center;
            align-self: flex-start;
            gap: 0.55rem;
            padding: 0.35rem 0.9rem;
            background: #F5F5F4;
            border: 1px solid rgba(0, 0, 0, 0.08);
            border-radius: 9999px;
            font-family: var(--font-mono, monospace);
            font-size: 0.7rem;
            font-weight: 600;
            letter-spacing: 0.12em;
            text-transform: uppercase;
            color: #78716C;
        }

        .purchase-eyebrow .badge-dot {
            width: 0.45rem;
            height: 0.45rem;
            border-radius: 50%;
            background: #22C55E;
            box-shadow: 0 0 8px rgba(34, 197, 94, 0.6);
            flex-shrink: 0;
        }

        .purchase-title {
            font-family: var(--font-sans, sans-serif);
            font-size: clamp(2rem, 3.2vw, 2.65rem);
            font-weight: 700;
            letter-spacing: -0.03em;
            color: #1C1C1C;
            line-height: 1.15;
            margin: 0;
        }

        .purchase-brief-desc {
            font-size: 0.95rem;
            line-height: 1.55;
            color: #70706E;
            margin: 0;
        }

        /* Preço e Lote */
        .purchase-pricing-row {
            display: flex;
            align-items: flex-end;
            justify-content: space-between;
            gap: 1rem;
            padding-bottom: 0.25rem;
        }

        .price-stack {
            display: flex;
            flex-direction: column;
            gap: 0.2rem;
        }

        .price-tiny-label {
            font-family: var(--font-mono, monospace);
            font-size: 0.65rem;
            font-weight: 600;
            letter-spacing: 0.08em;
            color: #8C8C88;
        }

        .price-main-val {
            font-family: var(--font-mono, monospace);
            font-size: 1.75rem;
            font-weight: 700;
            letter-spacing: -0.02em;
            color: #1C1C1C;
        }

        .batch-future-tag {
            font-family: var(--font-mono, monospace);
            font-size: 0.7rem;
            font-weight: 600;
            letter-spacing: 0.06em;
            color: #555552;
            background: #FFFFFF;
            border: 1px solid #D5D5D0;
            padding: 0.35rem 0.75rem;
            border-radius: 4px;
        }

        /* Caixa de Especificações Rápidas */
        .quick-specs-container {
            background: #F8F8F7;
            border: 1px solid #EFEFEA;
            border-radius: 6px;
            padding: 1.2rem 1.4rem;
        }

        .quick-specs-label {
            font-family: var(--font-mono, monospace);
            font-size: 0.68rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            color: #8C8C88;
            margin-bottom: 0.85rem;
        }

        .quick-specs-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.25rem;
        }

        .quick-spec-item {
            display: flex;
            flex-direction: column;
            gap: 0.3rem;
        }

        .spec-term {
            font-family: var(--font-mono, monospace);
            font-size: 0.65rem;
            font-weight: 600;
            letter-spacing: 0.04em;
            color: #8C8C88;
        }

        .spec-def {
            font-family: var(--font-sans, sans-serif);
            font-size: 0.88rem;
            font-weight: 600;
            color: #1C1C1C;
        }

        /* 2. Seleção de Modelos - EXATAMENTE 3 CARDS POR LINHA */
        .model-selection-wrapper {
            display: flex;
            flex-direction: column;
            gap: 0.6rem;
            width: 100%;
        }

        .selection-label {
            font-family: var(--font-mono, monospace);
            font-size: 0.68rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            color: #8C8C88;
        }

        .model-cards-row,
        .model-cards-row-more {
            display: grid !important;
            grid-template-columns: repeat(3, 1fr) !important;
            gap: 0.6rem !important;
            width: 100% !important;
        }

        .model-cards-row-more {
            margin-top: 0.4rem;
        }

        .hidden {
            display: none !important;
        }

        .model-selection-footer {
            display: flex;
            justify-content: flex-end;
            margin-top: 0.35rem;
            width: 100%;
        }

        .btn-toggle-more-models {
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
            background: none;
            border: none;
            padding: 0.25rem 0.1rem;
            font-family: var(--font-mono, monospace);
            font-size: 0.72rem;
            font-weight: 600;
            letter-spacing: 0.05em;
            color: #70706E;
            cursor: pointer;
            transition: color 0.2s ease;
        }

        .btn-toggle-more-models:hover {
            color: #1C1C1C;
        }

        /* Cards de Opções sem sublinhado azul */
        .model-select-card {
            width: 100%;
            min-width: 0;
            background: #FFFFFF;
            border: 1px solid #D8D8D4;
            border-radius: 4px;
            padding: 0.75rem 0.85rem;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            gap: 0.25rem;
            cursor: pointer;
            transition: all 0.2s ease;
            text-align: left;
            text-decoration: none !important;
            box-sizing: border-box;
        }

        .model-select-card:hover {
            border-color: #1C1C1C;
        }

        .model-select-name {
            font-size: 0.78rem;
            font-weight: 600;
            color: #1C1C1C !important;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            width: 100%;
            text-decoration: none !important;
        }

        .model-select-status {
            font-family: var(--font-mono, monospace);
            font-size: 0.68rem;
            color: #8C8C88 !important;
            text-decoration: none !important;
        }

        .model-select-card.active {
            background: #1C1C1C;
            border-color: #1C1C1C;
        }

        .model-select-card.active .model-select-name {
            color: #FFFFFF !important;
        }

        .model-select-card.active .model-select-status {
            color: #A5A5A0 !important;
        }

        /* Garantias */
        .purchase-guarantees {
            display: flex;
            flex-direction: column;
            gap: 0.6rem;
            padding-top: 0.5rem;
            border-top: 1px solid #EFEFEA;
        }

        .guarantee-item {
            display: flex;
            align-items: center;
            gap: 0.6rem;
            font-size: 0.8rem;
            color: #555552;
        }

        .guarantee-item svg {
            color: #1C1C1C;
        }

        /* Seção de Especificações Detalhadas */
        .product-specs-detailed-section {
            margin-top: 4rem;
            padding-top: 3rem;
            border-top: 1px solid #EAEAE7;
        }

        .specs-section-header {
            margin-bottom: 2rem;
        }

        .specs-section-title {
            font-family: var(--font-sans, sans-serif);
            font-size: 1.15rem;
            font-weight: 700;
            letter-spacing: 0.04em;
            text-transform: uppercase;
            color: #1C1C1C;
            margin: 0 0 0.3rem 0;
        }

        .specs-section-subtitle {
            font-family: var(--font-mono, monospace);
            font-size: 0.75rem;
            color: #8C8C88;
        }

        .specs-table-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 2rem;
            background: #F8F8F7;
            border: 1px solid #EFEFEA;
            border-radius: 6px;
            padding: 1.75rem 2rem;
        }

        @media (max-width: 640px) {
            .specs-table-grid {
                grid-template-columns: 1fr;
                gap: 1rem;
            }
        }

        .specs-table-col {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .spec-row {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
            padding-bottom: 0.75rem;
            border-bottom: 1px solid #EAEAE5;
        }

        .spec-row:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .spec-key {
            font-family: var(--font-mono, monospace);
            font-size: 0.68rem;
            font-weight: 700;
            letter-spacing: 0.05em;
            text-transform: uppercase;
            color: #8C8C88;
        }

        .spec-val {
            font-size: 0.88rem;
            font-weight: 600;
            color: #1C1C1C;
        }

        .product-long-description-box {
            margin-top: 2rem;
            line-height: 1.65;
            color: #555552;
            font-size: 0.92rem;
        }

        /* 3. POP-UP MODAL FIXO DE CAPTAÇÃO (AVISE-ME) */
        .aviseme-modal-backdrop {
            position: fixed !important;
            inset: 0 !important;
            z-index: 99999 !important;
            background: rgba(0, 0, 0, 0.6) !important;
            backdrop-filter: blur(10px) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            display: flex !important;
            align-items: center !important;
            justify-content: center !important;
            padding: 1.5rem !important;
        }

        .aviseme-modal-backdrop.hidden {
            display: none !important;
        }

        .aviseme-modal-card {
            position: relative;
            background: #FFFFFF;
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            padding: 2.2rem 2rem;
            max-width: 440px;
            width: 100%;
            box-shadow: 0 25px 60px -12px rgba(0, 0, 0, 0.35);
            color: #1C1C1C;
            animation: modalPopIn 0.25s ease-out;
        }

        @keyframes modalPopIn {
            from {
                opacity: 0;
                transform: scale(0.95);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .modal-close-btn {
            position: absolute;
            top: 1.1rem;
            right: 1.1rem;
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: #F4F4F2;
            border: 1px solid rgba(0, 0, 0, 0.08);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #1C1C1C;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .modal-close-btn:hover {
            background: #E5E5E0;
            transform: scale(1.08);
        }

        .modal-content-state {
            display: flex;
            flex-direction: column;
            gap: 0.9rem;
        }

        .modal-content-state.hidden {
            display: none !important;
        }

        .modal-eyebrow {
            display: inline-flex;
            align-items: center;
            gap: 0.45rem;
            font-family: var(--font-mono, monospace);
            font-size: 0.65rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            color: #70706E;
            text-transform: uppercase;
        }

        .modal-eyebrow .badge-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: #22C55E;
        }

        .popup-title {
            font-size: 1.35rem;
            font-weight: 800;
            letter-spacing: -0.025em;
            color: #1C1C1C;
            margin: 0;
        }

        .modal-desc {
            font-size: 0.85rem;
            line-height: 1.5;
            color: #70706E;
            margin: 0;
        }

        .aviseme-form {
            display: flex;
            flex-direction: column;
            gap: 0.9rem;
            margin-top: 0.5rem;
        }

        .form-field {
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
            text-align: left;
        }

        .field-label {
            font-family: var(--font-mono, monospace);
            font-size: 0.68rem;
            font-weight: 600;
            letter-spacing: 0.05em;
            text-transform: uppercase;
            color: #70706E;
        }

        .field-input {
            width: 100%;
            padding: 0.75rem 0.95rem;
            border: 1px solid rgba(0, 0, 0, 0.15);
            border-radius: 4px;
            font-size: 0.88rem;
            font-family: var(--font-sans, sans-serif);
            color: #1C1C1C;
            background: #FAFAFA;
            outline: none;
            box-sizing: border-box;
            transition: all 0.2s ease;
        }

        .field-input:focus {
            border-color: #1C1C1C;
            background: #FFFFFF;
            box-shadow: 0 0 0 2px rgba(28, 28, 28, 0.1);
        }

        .modal-submit-btn {
            width: 100%;
            padding: 0.85rem 1rem;
            background: #1C1C1C;
            color: #FFFFFF;
            border: 1px solid #1C1C1C;
            border-radius: 4px;
            font-family: var(--font-sans, sans-serif);
            font-size: 0.78rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.6rem;
            cursor: pointer;
            transition: all 0.2s ease;
            margin-top: 0.3rem;
        }

        .modal-submit-btn:hover {
            background: #000000;
            transform: translateY(-1px);
        }

        .success-icon-badge {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: #DCFCE7;
            color: #16A34A;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0.5rem auto;
        }
    
        .specs-description-row {
            grid-column: 1 / -1;
            padding-top: 1.25rem;
            margin-top: 0.5rem;
            border-top: 1px dashed #E5E5E0;
            display: flex;
            flex-direction: column;
            gap: 0.45rem;
        }

        .specs-description-text {
            font-family: var(--font-sans, sans-serif);
            font-size: 0.88rem;
            line-height: 1.6;
            color: #444441;
            margin: 0;
        }
    </style>